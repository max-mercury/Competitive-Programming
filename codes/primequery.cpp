#include <iostream>
using namespace std;

int main ()
{
    int n,temp,q;
    cin>>n;
    for (i=0;i<n;i++)
    {
        cin >> a[i];
    }
    cin >> q;
    for (i=0;i<q;i++)
    {
        cin >> l[i] >> r[i] >> x[i]>> y[i];
    }
    for (i=0;i<n;i++)
    cout << a[i]<<endl;;
    for (i=0;i<q;i++)
    {
        cout << l[i] << r[i] << x[i] << y[i]<<endl;;
    }
    return 0;
    }
