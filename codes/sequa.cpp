#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int t;
    int a[100],c;
    double p1,p2,p3,p;
    int n,m;
    double ans,sum=0;
    double i;
    cin >> t;
    while(t--)
    {
        cin >> n >> m;
       for(i=0;i<=n;i++)
       {
            p=pow(i,i);
        }
        ans = fmod(p,m);
        cout << ans << endl;
    }
cin.get();
cin.get();
return 0;    
}
